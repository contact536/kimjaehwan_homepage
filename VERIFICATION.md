> 이전 원본 재현판의 기록입니다. 현재 개인화 변경 내용과 검증 범위는 PERSONALIZATION.md 및 README.md를 참고하세요.

# 검증 기록

2026-09-06, Node.js 24.11.1, Windows에서 검증했습니다.

- 원본 HTML 33개 + 관리자 1개: 로컬 HTTP 200.
- 외부 파일 및 인라인 JavaScript 구문 검사 10건 통과.
- TypeScript 검사 통과.
- API 통합 테스트 2개와 원본 재현 테스트 5개, 총 7개가 통과했습니다. 원본 파일 39개의 SHA-256 일치, 기본 HTML 응답의 바이트 일치, 편집 데이터의 안전한 직렬화, 프로필·뉴스 삽입 이스케이프, 중첩 메뉴 호환을 추가 검증합니다. 기존 API 검증에는 초기 893개 레코드, 로그인·쓰기 보호, 입력 검증, 수정 충돌, 데이터 영속성과 로그인 제한 등이 포함됩니다.
- Drizzle 생성 SQL은 테이블 3개와 복합 인덱스 1개를 생성하는 스키마 전용 마이그레이션입니다. 대량 seed는 마이그레이션에 포함하지 않았습니다.
- 의존성 감사: 보안 override를 반영해 설치한 트리에서 npm audit 취약점 0건. Drizzle 하위 esbuild의 구버전을 직접 개발 의존성의 패치 버전으로 통일했습니다.
- Hono 서버의 Workers 빌드 성공. 생성한 번들은 node:sqlite를 포함하지 않으며 로컬 전용 adapter와 분리됩니다.

실제 브라우저 화면·모바일·스크린리더 테스트, 픽셀 비교, Lighthouse 성능 수치는 이번 범위에 포함하지 않았습니다. 외부 사진·캘린더·폰트의 지속적인 가용성도 보장하지 않습니다.

프론트엔드에는 원본에 없는 WebMCP 등록 코드나 새 검색 렌더러를 삽입하지 않습니다. 기존 비공개 배포는 이번 분리본으로 교체하지 않았습니다.
