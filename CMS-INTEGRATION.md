# 研究 본문 CMS 통합 · 실행 안내

현재 작업 프로젝트: sjhwang-faithful. sjhwang-platform의 구조를 참고해 기존 디자인과 LangGraph·Ollama를 유지하며 통합했습니다.

## 사용하는 방법
1. http://127.0.0.1:4321/admin/ 에 로그인합니다.
2. 연구 본문 컬렉션에서 CLOA, TAXiA, KorTaxArena 중 하나를 선택합니다.
3. 연구 이름·부제·목록 요약·연구 본문을 수정하고 저장합니다.
4. 홈, 프로젝트, 소개, 연구 상세의 연결된 필드와 KorTaxArena 자료 요약에 반영됩니다. 연구 도우미의 자료 검색과 AI 도구도 같은 DB를 읽습니다.

기존 세 연구의 수정만 지원합니다. 새 연구 생성·삭제, 분야 태그, 출처, 질문별 탐색의 설명은 별도 콘텐츠 생성 자료에서 관리합니다. 자동 저장은 없습니다. 다른 곳에서 수정한 항목은 재조회해야 하며 원본 DB는 data/에 백업했습니다.

## 프론트엔드 ZIP 내보내기
프로젝트 폴더에서 npm run build:frontend 를 실행하면 deliverables/frontend-날짜/와 동일 이름의 ZIP이 생성됩니다. 매번 새 폴더를 만들며 이전 ZIP을 덮어쓰지 않습니다.
현재 저장된 공개 데이터를 HTML에 반영합니다. 관리자 화면·비밀번호·DB·모델 설정은 포함하지 않습니다. 자료 검색/AI 도우미는 백엔드 기능이므로 정적 묶음에서는 안내 페이지로 대체합니다. ZIP을 풀어 HTTP 서버의 루트로 실행해야 합니다(file:// 직접 열기 미지원).

Windows ZIP 생성은 .NET 압축 API를 사용합니다. 예: python -m http.server 14318 --bind 127.0.0.1

## 구현과 검증
research-cms-v1 초기화는 기존 데이터를 보존하고 연구 3건만 한 번 추가합니다. Zod 입력 검증·로그인·수정 버전 충돌 방지를 유지합니다. 입력된 HTML은 텍스트로 출력됩니다.

서버: research-content.ts, database.ts, validation.ts, original.ts, agent.ts, app.ts.
프론트: public/admin/, scripts/bind-research-content.mjs.
내보내기: scripts/export-frontend.mjs.

테스트 15개, TypeScript 검사와 빌드 통과. 저장→페이지→자료 검색→LangGraph 도구 연결을 격리된 DB에서 검증했습니다. 실제 로컬 API도 확인했으며 운영 데이터에 테스트 문구를 저장하지 않았습니다. ZIP 63개 파일을 원본과 SHA-256 비교했습니다. 재배포 및 브라우저 시각 검증은 수행하지 않았습니다.

## 관리자 비밀번호 변경
관리자 로그인 후 상단의 ‘관리자 비밀번호 변경’을 펼칩니다. 현재 비밀번호와 8~128자의 새 비밀번호·확인을 입력합니다. 성공하면 모든 기존 세션이 종료되며 새 비밀번호로 다시 로그인합니다.

변경 값은 settings 테이블에 솔트와 PBKDF2-SHA256 해시로 저장되어 재시작 후에도 유지됩니다. 변경 이후에는 .env의 초기 ADMIN_PASSWORD보다 DB 설정이 우선합니다. 콘텐츠 내보내기와 프론트엔드 ZIP에는 인증 설정이 포함되지 않습니다. 로컬과 배포 사이트는 서로 다른 DB이므로 비밀번호 변경도 해당 사이트에만 적용됩니다.


## 프로젝트 분석하기 세션 비교 및 추가 통합 (2026-09-06)
원본: ../sjhwang-platform/public/admin/admin.js. 연구 CMS와 Hono/SQLite/Zod는 이미 통합되어 있으므로 DB를 복제하거나 교체하지 않았습니다.
추가 적용: 컬렉션별 안전한 텍스트 미리보기, 미저장 상태 및 이탈 확인, 수정 취소, 저장/삭제/비밀번호 변경 중 편집 전환 차단, 목록 오류 재시도, 삭제 후 마지막 페이지 보정.
원본의 focus/method 연구 필드는 현재 플랫폼의 body에 맞추었습니다. 최신 비밀번호 변경·8자 규칙·LangGraph 연구 도우미와 리비전 충돌 보호를 유지합니다.
GitHub Pages에는 백엔드와 관리자 화면을 게시하지 않습니다. 이 변경은 로컬 전체 플랫폼의 /admin/에서 사용합니다.
검증: 관리자 VM 테스트에서 미리보기 HTML 비실행, 취소된 이탈의 내용 유지, 수정 취소, 저장 중 전환 차단 및 409 실패 후 입력 유지 확인.

공개 화면 추가: 원본 site.js의 페이지 찾기 패턴을 적용했습니다. 11개 주요 목적지를 검색하며 연구 3개는 상세 섹션으로 연결합니다. 네트워크 요청 없이 정적 HTML 링크를 필터링하므로 GitHub Pages에서도 사용할 수 있습니다. content:generate의 마지막 단계에서 재생성합니다. 이번 요청의 대상인 로컬 홈페이지에 반영했으며 공개 사이트 재배포는 하지 않았습니다. 테스트 18개 및 타입 검사·빌드 통과.
